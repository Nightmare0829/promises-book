import Rule from "../rule";
export default class Diff {
    pattern: RegExp;
    expected: string;
    index: number;
    matches: string[];
    rule: Rule;
    constructor(pattern: RegExp, expected: string, index: number, matches: string[], rule?: Rule);
    tailIndex: number;
    isEncloser(other: Diff): boolean;
    isCollide(other: Diff): boolean;
    isBefore(other: Diff): boolean;
}
